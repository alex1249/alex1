# 설계 메모

## 왜 테이블을 3개로 나눴나
- 과목이 늘어도 구조를 바꿀 필요가 없게 하려고 (subjects 분리)
- 정리본 하나에 원본자료가 여러 개 붙는 1:다 관계를 표현하려고 (materials ↔ originals)
- 한 테이블에 다 넣으면 같은 과목명이 행마다 중복되고, 원본이 늘 때마다 컬럼을 추가해야 함

## 왜 FK를 썼나
- 과목명을 정리본마다 텍스트로 복붙하면 오타 하나로 관계가 깨짐
- subject_id / material_id 숫자 하나로 관계를 고정

## URL 파라미터를 FK 조회 입력으로 사용
- /subjects/1 요청 → subject_id=1 → materials를 그 값으로 필터링
- 라우팅과 데이터 관계가 그대로 대응됨

## 기타
- row_factory = sqlite3.Row: 템플릿에서 컬럼명으로 접근 가능
- 쿼리에 ? 파라미터 바인딩: SQL 인젝션 방지